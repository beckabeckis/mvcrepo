{% extends "base.html.twig" %}

{% block title %}Home{% endblock %}

{% block body %}
    <h1>Session Deleted</h1>
    <p>CardHand: {{ dump(cardHand) }}</p>
    <p>DeckOfCards: {{ dump(deckOfCards) }}</p>

    {{ include('flash.html.twig') }}
{% endblock %}