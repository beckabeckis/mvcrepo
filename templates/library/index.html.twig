{% extends 'base.html.twig' %}

{% block title %}Hello LibraryController!{% endblock %}

{% block body %}
<h1>Bibliotek</h1>
<ul>

	{% for routeName,route in routes %}
		<li>
			<a href="{{ path(route) }}">
				<p>{{ routeName }}</p>
			</a>
		</li>
	{% endfor %}
</ul>

{% endblock %}
